resources:
  - ../../base
  - patch.yaml

patches:
  - target:
      group: apps
      version: v1
      kind: Deployment
      name: ingress-nginx-controller
      namespace: ingress-nginx
    path: patch-container-resources.yaml
